---
layout: post
title:  "云安全流水线架构"
date:   2026-07-13
tags:
    - 云安全架构
    - CI/CD
    - 流水线各阶段攻击
---

# 容器镜像全生命周期安全风险手册

## 概述

本文档梳理容器镜像从源代码到生产运行全链路的安全风险与缓解措施。
架构图等我上传吧

**镜像生命周期总览：**
构建前（配置/依赖文件） → 构建（生成镜像） → 存储（镜像仓库） → 部署（运行容器）


---

## 阶段1：镜像构建前（Git仓库）

### 数据内容

Git仓库存储构建所需的原材料：
- 业务代码
- 配置文件
- 依赖清单
- CI/CD流水线定义文件（如 `.gitlab-ci.yml`）
- Dockerfile

### 数据流向

构建机从Git仓库拉取这些原材料，进入构建环节。

---

## 风险1.1：源代码控制被入侵

**含义：** 攻击者通过盗取开发者账号、利用仓库漏洞或提交恶意代码（如后门），可直接篡改或窃取Git仓库中的核心业务代码与配置等。

**真实事故：** Grafana Labs勒索事件。攻击者对TanStack npm供应链攻击后利用一个被忽视的凭证登录Grafana Labs GitHub代码仓库，下载源码后以泄露数据为威胁，索要赎金。

> 参考链接：https://grafana.com/blog/grafana-labs-security-update-latest-on-tanstack-npm-supply-chain-ransomware-incident/

**缓解措施：**
- 启用多因素认证（MFA）并严格管理账号权限
- 使用短期有效的轮换Token替代长期凭证，并遵循最小权限原则
- 建立应急响应计划：全局代码冻结、跨平台日志审计、建立机制确保能发现并轮换所有类型的凭证

---

## 风险1.2：敏感信息泄露

**含义：** 开发者不慎将API密钥、数据库密码、证书等敏感信息以明文形式提交到代码仓库中，即使后续删除，历史记录中依然存在。

**真实事故：** Permify项目API密钥泄露。开发者在`buf.yaml`中意外提交了明文API密钥，虽被移除但已记录在Git历史中，存在被滥用的风险。

> 参考链接：https://blog.gitcode.com/40859ed5d0f1ac0cbc1d7326613bbc69.html

**缓解措施：**
- 将敏感信息从代码库中完全移除，改为通过环境变量在运行时注入，或使用专用的密钥管理服务（如AWS Secrets Manager、HashiCorp Vault）
- Pre-commit hook扫描拦截含敏感信息的提交
- 若已泄露，需立即撤销密钥并清理Git历史

---

## 风险1.3：有漏洞的代码

**含义：** 业务代码中含有漏洞。

**真实事故：** MOVEit文件传输软件遭勒索攻击（CVE-2023-35708），攻击者利用MOVEit软件中的一个SQL注入漏洞大规模数据窃取攻击，该漏洞允许未经身份验证的攻击者直接访问和窃取其后端数据库中的内容。

> 参考链接：https://www.theregister.com/security/2023/06/17/third-moveit-bug-fixed-a-day-after-poc-exploit-made-public/1010133

**缓解措施：**
- SAST静态安全代码扫描，安全左移，扫描后修复

---

## 风险1.4：有漏洞的依赖

**含义：** 依赖清单引用的第三方库、组件等和漏洞数据库比对有漏洞。

**真实事故：** 攻击者利用Apache Struts 2漏洞在HTTP请求头中注入OGNL表达式执行任意代码。Equifax（美国征信巨头）在2017年因未升级该依赖，导致约1.47亿用户敏感信息泄露，公司赔偿超7亿美元。

> 参考链接：https://www.secrss.com/articles/12448

**缓解措施：**
- **依赖锁定**：使用`package-lock.json`、`Pipfile.lock`等锁定文件，确保每次安装依赖的版本完全一致，防止意外引入被投毒的最新版本
- **来源验证**：仅从官方认证的仓库（如官方npm源、PyPI）拉取依赖，配置私有代理仓库，并使用`package-lock.json`锁定源码hash值进行校验
- **SCA软件成分分析**
- 发现后更新依赖版本：升级到已修复漏洞的安全版本

---

## 风险1.5：错误配置

**含义：** 指存储在Git仓库中的静态配置文件本身存在安全缺陷。

**真实安全事故：** 攻击者通过恶意构造`.gitmodules`文件，利用Git配置解析器处理回车符（`\r`）时的逻辑不一致，触发了路径穿越漏洞，最终可实现远程代码执行（RCE）。当用户执行`git clone --recursive`克隆该恶意仓库时，Git会将子模块文件写入错误目录，从而在用户机器上执行攻击者预设的恶意代码。

> 参考链接：https://dgl.cx/2025/07/git-clone-submodule-cve-2025-48384

**缓解措施：**
- **代码审查**：强制要求在提交代码前进行扫描，阻止包含已知漏洞模式的文件（如旧版易受攻击的脚本）进入主分支
- **升级Git客户端**：官方常会在新版本中修复漏洞
- **自动化IaC扫描**：基于安全基线扫描

---

## 阶段2：镜像构建（构建机）

### 数据内容

构建机按照CI/CD流水线文件调度构建任务，按Dockerfile指令，基于从Git仓库拉取的代码和配置，从互联网拉取相关依赖，最终打包成容器镜像（静态只读）并推送。

### 数据流向

构建完成后，构建机将镜像推送到镜像仓库。

---

## 风险2.1：构建机被入侵

**含义：** 作为生成镜像的核心节点，如果构建机本身被植入木马或恶意脚本，那么生成的每一个镜像都将被污染。

**真实事故：** 攻击者（被指认为APT29）入侵了SolarWinds的构建服务器。他们植入了一个名为SUNSPOT的恶意软件，该软件会监控构建过程，并在编译Orion软件时，自动向源码中注入SUNBURST后门。

> 参考链接：https://www.app-attack-matrix.com/attacks/#teamtnt-docker-gatling-gun-campaign

**缓解措施：**
- 将构建机作为隔离的"不可变基础设施"管理，定期重建，禁止人工随意登录
- 使用临时、干净的构建Runner（如云原生构建池），任务结束后销毁

---

## 风险2.2：篡改源码或配置

**含义：** 攻击者通过盗取开发者账号、利用仓库漏洞或提交恶意代码（如后门），直接篡改或窃取Git仓库中的核心业务代码、配置文件等源码资产。篡改后的源码会在后续构建过程中被编译进最终交付物，影响所有下游用户。

**真实事故：** TeamPCP Megalodon攻击，在6小时内向5000多个GitHub仓库注入恶意Actions工作流，窃取AWS/GCP密钥和SSH密钥。

> 参考链接：https://www.infostealers.com/article/infostealers-just-spawned-a-5000-repo-github-supply-chain-attack/

**缓解措施：**
- **分支保护与代码审查**：在Git平台（GitHub/GitLab）强制启用主分支保护，要求所有代码合并必须经过Pull Request和至少一人审核，禁止直接push
- 进行相应的合规检查：根据相应安全基线，定期审计commit历史和仓库访问日志，发现异常提交或访问行为立即告警

---

## 风险2.3：CI/CD攻击

**含义：** 攻击者利用CI/CD流水线自身的配置缺陷（如权限过大、动态输入未过滤、第三方Action被劫持），在构建阶段注入恶意脚本或篡改构建产物（如二进制文件、Docker镜像），获取敏感信息等。这类攻击不直接修改源码仓库，而是污染构建环境或流程，导致从该流水线产出的所有交付物被植入后门等。

**真实事故：** Hackerbot-claw供应链攻击，利用`pull_request_target`触发器和分支名注入恶意载荷，外传GitHub令牌。

> 参考链接：https://www.secrss.com/articles/88160

**缓解措施：**
- **固定第三方Action的SHA**：不使用易变的标签（如`@v4`），而是固定到具体的40位commit SHA
- **最小权限原则**：限制CI/CD令牌的权限，禁止`write-all`，仅授予该流水线所需的最少权限（如只读特定仓库、仅推送至特定镜像仓库）
- **限制出站网络**：CI运行器应限制出站连接，仅允许访问可信的包管理器和镜像仓库，防止数据外泄到攻击者控制的服务器
- **审计与监控**：定期审计runner日志和构建产物，警惕异常的构建延迟、陌生的分支名触发的构建或构建产物大小的异常变化

---

## 风险2.4：依赖项混淆

**含义：** 攻击者利用包管理器的命名空间或版本解析机制缺陷，诱导开发者安装恶意包。主要包括两种方式：

1. **依赖混淆**：攻击者在公共仓库（如npm官方源）上传一个与私有包同名的高版本恶意包。当包管理器配置不当（如同时使用公共源和私有源）时，可能会从公共源拉取更高版本的恶意包，而非预期的内部私有包。
2. **拼写混淆（Typosquatting）**：攻击者发布与流行库名称非常相似的恶意包（如`lodash-utils`冒充`lodash-utils-secure`，或`axios`的变种）。开发者因手误或自动补全而错误引入。

**真实事故：** Ownit电信公司依赖混淆攻击，一个针对北欧电信和数字服务组织的攻击活动。攻击者在npm上发布了名为`@ownit/core`的恶意包，版本号设为99.0.0，高于其内部私有包。该包在安装时通过`preinstall`钩子收集系统信息（主机名、用户名、环境变量、CI/CD令牌等）并外传。

> 参考链接：https://osv.dev/vulnerability/MAL-2026-5155

**缓解措施：**
- **使用Scope命名空间**：对于私有包，使用`@your-company/package-name`的命名空间格式，防止与公共包冲突
- **拼写检查与人工审查**：在添加新依赖时，仔细核对包名拼写；使用`npm audit`或`pnpm audit`检查依赖来源
- **依赖锁定与来源验证**：使用`package-lock.json`锁定依赖的精确版本和源码hash，确保安装的包与预期一致

---

## 风险2.5：有漏洞的依赖（构建阶段）

**含义：** 开发者通过依赖文件（如`package.json`、`requirements.txt`）正常引入的第三方库，其特定版本存在已知安全漏洞（CVE），或被攻击者通过劫持维护者账户、篡改发布流程等方式植入了恶意代码。这类风险不涉及包名混淆，而是合法包本身被污染或存在缺陷。

**真实事故：** 月下载量超9500万的AI基础设施库LiteLLM被攻击者植入恶意版本。该版本包含一个恶意的`.pth`文件，会在Python解释器每次启动时自动窃取SSH密钥、云服务凭证和数据库密码。

> 参考链接：https://www.trendmicro.com/zh_tw/research/26/c/your-ai-stack-just-handed-over-your-root-keys-inside-the-litellm-pypi-breach.html

**缓解措施：**
- **自动化漏洞扫描**：在CI/CD流水线中集成Snyk、Trivy等工具，自动检测依赖中的已知CVE漏洞和可疑行为（如postinstall脚本、异常网络请求）
- **依赖锁定**：使用`package-lock.json`、`Pipfile.lock`等锁定文件，防止意外引入被投毒的最新版本
- **延迟更新策略**：对关键依赖设置`minimumReleaseAge`，新发布的包需等待24小时或一周再允许安装，避开刚发布即可能被下架的恶意版本

---

## 阶段3：镜像存储（镜像仓库）

### 数据内容

镜像仓库存储构建机推送的镜像。

### 数据流向

部署环境宿主机从镜像仓库拉取镜像，准备部署。

---

## 风险3.1：镜像仓库被入侵

**含义：** 攻击者通过漏洞利用、凭证窃取或内部威胁等方式，直接入侵镜像仓库（如Docker Hub、Harbor、Quay），篡改、替换或删除仓库中的镜像，植入后门或挖矿程序。这属于仓库系统本身被攻破。

**真实事故：** Quay镜像仓库权限提升漏洞（CVE-2025-4374）：Red Hat Quay容器镜像仓库存在一个权限控制缺陷。当组织启用代理缓存功能，用户拉取尚未缓存的镜像时，系统会自动创建仓库并默认授予该用户管理员权限，绕过了正常的权限校验流程。任何认证用户均可利用此漏洞获得任意新创建仓库的管理控制权。

> 参考链接：https://vuldb.com/cve/CVE-2025-4374

**缓解措施：**
- **使用私有镜像仓库**：建立内部受控的镜像仓库（如Harbor、AWS ECR），禁止生产环境直接从公网拉取镜像
- **镜像签名与校验**：启用Docker Content Trust等对镜像进行签名，部署时强制验证签名和摘要（digest），拒绝未签名或签名不匹配的镜像
- **扫描与准入控制**：在镜像推送和拉取时集成漏洞扫描工具，阻断包含高危漏洞或恶意软件的镜像进入仓库
- **禁用可变标签，固定摘要**：在部署配置中使用镜像的SHA256摘要（如`image@sha256:...`），而非可变标签（如`latest`），防止标签被重定向到恶意镜像

---

## 风险3.2：镜像篡改

**含义：** 攻击者直接攻破仓库（比如Docker Hub、Harbor），拿到管理员权限后，篡改里面的镜像。镜像构建存储分发中，植入恶意代码、后门程序或替换合法组件。

**真实事故：** OneAPI AI网关Docker镜像被植入挖矿脚本，拥有2万GitHub Star的开源AI网关工具OneAPI，其Docker Hub镜像被植入了XMR门罗币挖矿脚本。原因是Docker Hub的推送密钥（Token）泄露，导致黑客向多个镜像版本（包括v0.6.5和v0.6.9）重新推送了被污染的镜像。用户安装后，CPU占用率会异常升至约50%，且容器会与矿池服务器（107.167.83.34:443）建立连接。

> 参考链接：https://my.oschina.net/u/3874284/blog/17936324

**缓解措施：**
- **私有仓库与准入控制**：建立私有镜像仓库，生产环境禁止直接从公网拉取；在仓库或K8s准入层集成扫描工具（如Trivy），自动阻断含恶意代码或高危漏洞的镜像
- 清理runner和镜像代理的历史缓存，防止恶意digest被无声复用

---

## 阶段4：镜像运行（部署环境）

### 数据内容

部署环境的宿主机拉取镜像后，启动运行时容器。

---

## 风险4.1：有漏洞的主机

**含义：** 宿主机操作系统存在已知安全漏洞，攻击者可能获得主机权限并进行横向移动或拒绝服务攻击。

**真实事故：** 中国国家计算机病毒应急处理中心与360联合发布调查报告，披露了美国国家安全局（NSA）对中国重要基础设施和主机的长期渗透。攻击方利用多个未公开的0-day漏洞和网络设备漏洞，控制了高校西北工业大学内网跳板机及核心服务器。

> 参考链接：https://www.secrss.com/articles/54025

**缓解措施：**
- **及时升级**：将runc、containerd、Kubernetes等组件升级到修复版本（runc ≥ v1.2.8 / v1.3.3）
- **使用User Namespace**：以非root用户运行容器，阻止攻击者访问宿主机的敏感`/proc`文件
- **自动化运维**：使用GitOps管理配置，禁止手动修改主机，所有代码和配置都存储在代码控制下，并使用自动化流程进行更新

---

## 风险4.2：网络攻击

**含义：** 底层网络配置存在漏洞，攻击者借此进行横向渗透或拒绝服务攻击。容器通常需要与外界通信，若网络隔离不足，风险极高。

**真实事故：** Kubernetes externalIPs中间人攻击漏洞（CVE-2020-8554），该漏洞已存在六年，允许任何有Service创建权限的用户通过设置`externalIPs`字段劫持集群流量。例如，攻击者可声明8.8.8.8为externalIP，将集群内所有DNS查询拦截到恶意Pod。

> 参考链接：https://atbug.com/kubernetes-externalips-security-vulnerability-deprecation/

**缓解措施：**
- **实施微隔离**：使用Kubernetes NetworkPolicy定义精细的Pod间通信规则，默认拒绝所有流量，仅放行必要服务
- **限制出站访问**：通过防火墙或服务网格限制容器对外部网络的访问

---

## 风险4.3：篡改部署配置

**含义：** 攻击者在部署阶段篡改K8s Deployment、Service等配置，如修改镜像地址、开启特权模式或挂载敏感目录。

**真实事故：** 利用Ingress-NGINX控制器的严重漏洞（CVE-2025-1974），攻击者无需认证即可发送恶意请求通过向验证Webhook发送恶意请求，将任意配置注入到Nginx中，最终在控制器Pod内实现远程代码执行（RCE）。

> 参考链接：https://traditional-chinese.opswat.com/blog/ingressnightmare-cve-2025-1974-remote-code-execution-vulnerability-remediation

**缓解措施：**
- **使用GitOps管理部署配置**：将所有Kubernetes清单（Deployment、Service、ConfigMap等）存储在Git仓库中，通过ArgoCD或Flux等工具自动同步。任何配置变更都必须经过Pull Request和代码审查，禁止直接`kubectl apply`修改生产环境资源
- **启用准入控制器强制执行安全策略**：使用OPA/Gatekeeper或Kyverno等配置准入策略，在部署时自动拦截不安全配置

---

## 风险4.4：运行时漏洞利用

**含义：** 容器启动后，攻击者利用docker或containerd本身漏洞在容器内获取控制权。

**真实事故：** CVE-2026-31431 "Copy Fail"内核漏洞（2026年）：这是一个Linux内核自2017年起存在的严重漏洞，允许一个容器内的非特权进程通过AF_ALG加密套接字和`splice()`系统调用，直接篡改同一节点上其他容器共享的页面缓存中的文件（如`/usr/bin/cat`），实现跨容器代码执行，且不留下磁盘痕迹。

> 参考链接：https://www.stream.security/post/cve-2026-31431-how-copy-fail-behaves-in-kubernetes

**缓解措施：**
- **运行时安全防护检测**：部署eBPF运行时监控，使用产品监控容器内的异常系统调用模式，例如检测AF_ALG套接字（可识别CVE-2026-31431利用）或可疑的符号链接操作（可识别runC逃逸尝试）
- 升级docker或containerd到安全修复后的版本

---

## 风险4.5：敏感信息泄露（运行时）

**含义：** 容器日志、环境变量或挂载的卷中暴露了密钥等敏感数据。

**真实事故：** Docker Hub超万个镜像泄露实时云凭证（2025年）：安全公司Flare发现，Docker Hub上超过10,000个公开容器镜像泄露了至少一个敏感密钥，涉及OpenAI/HuggingFace的AI模型访问令牌、生产系统凭证、CI/CD数据库密码等。泄露源头是构建时捕获了`.env`文件、硬编码API密钥或JSON配置，而这些泄露的凭证中有约75%在从镜像删除后仍保持有效，未被撤销。

> 参考链接：https://starmap.dbappsecurity.com.cn/info/13541

**缓解措施：**
- **禁止在日志中打印敏感信息**：在应用代码和CI/CD流程中，明确禁止使用`print(password)`、`console.log(apiKey)`等操作。配置日志脱敏（Log Masking）规则，自动过滤密钥、Token等敏感字符模式
- **严格管控挂载卷的权限**：避免将宿主机敏感目录（如`~/.aws`、`~/.kube`）挂载到容器内。若需挂载，应设置为只读模式并限制访问路径范围
- **使用运行时敏感信息管理工具**：避免静态凭证：不在镜像或环境变量中硬编码密钥，使用HashiCorp Vault Agent Sidecar、AWS Secrets Manager等工具，在Pod启动时动态注入短期有效的临时凭证
- **使用工作负载身份**：使用云厂商的IAM Role for ServiceAccount（如AWS IAM Roles for Service Accounts, Azure Workload Identity），为Pod分配临时、限权的云身份，避免使用长期访问密钥

---

## 风险4.6：容器逃逸

**含义：** 攻击者突破容器的命名空间隔离，获取宿主机（Host）的root权限。常见途径包括内核漏洞、错误配置（如共享PID命名空间、挂载`/dev`）、特权模式等。

**真实事故：** runc三连逃逸漏洞（CVE-2025-31133 / CVE-2025-52565 / CVE-2025-52881），2025年11月，runc被披露三个高危漏洞，影响1.2.7及更早、1.3.2及更早等版本。攻击者可利用符号链接替换和竞态条件，绕过maskedPaths或`/dev/console`挂载校验，实现完整容器逃逸。漏洞修复版本为v1.2.8、v1.3.3和v1.4.0-rc.3。

> 参考链接：https://www.alibabacloud.com/help/zh/ack/product-overview/security-advisory-for-cve-2025-31133-cve-2025-52565-and-cve-2025-52881

**缓解措施：**
- **权限裁剪**：禁用特权模式（`--privileged`），使用`--cap-drop=ALL`剥夺所有Linux能力，仅保留必要能力
- **强制隔离**：启用User Namespace隔离，避免容器root映射宿主机root；禁止共享宿主机敏感目录（如`/proc`、`/sys`、`/dev`）
- **运行时监控与防护**：使用Falco等工具监控容器内异常系统调用（如`mount`、`open_by_handle_at`）；启用eBPF或RuntimeClass增强隔离；部署容器安全服务（如腾讯云TCSS）自动拦截逃逸行为

---

## 附录：风险速查表

| 阶段 | 风险编号 | 风险名称 | 核心缓解策略 |
|------|----------|----------|-------------|
| 阶段1 | 1.1 | 源代码控制被入侵 | MFA、轮换Token、应急响应 |
| 阶段1 | 1.2 | 敏感信息泄露 | 密钥管理、Pre-commit扫描 |
| 阶段1 | 1.3 | 有漏洞的代码 | SAST扫描 |
| 阶段1 | 1.4 | 有漏洞的依赖 | 依赖锁定、来源验证、SCA |
| 阶段1 | 1.5 | 错误配置 | 代码审查、IaC扫描 |
| 阶段2 | 2.1 | 构建机被入侵 | 不可变基础设施、临时Runner |
| 阶段2 | 2.2 | 篡改源码或配置 | 分支保护、PR审核 |
| 阶段2 | 2.3 | CI/CD攻击 | 固定Action SHA、最小权限、网络限制 |
| 阶段2 | 2.4 | 依赖项混淆 | Scope命名空间、锁定文件 |
| 阶段2 | 2.5 | 有漏洞的依赖 | 自动化扫描、延迟更新 |
| 阶段3 | 3.1 | 镜像仓库被入侵 | 私有仓库、镜像签名、固定摘要 |
| 阶段3 | 3.2 | 镜像篡改 | 准入控制、缓存清理 |
| 阶段4 | 4.1 | 有漏洞的主机 | 及时升级、User Namespace、GitOps |
| 阶段4 | 4.2 | 网络攻击 | 微隔离、出站限制 |
| 阶段4 | 4.3 | 篡改部署配置 | GitOps、准入控制器 |
| 阶段4 | 4.4 | 运行时漏洞利用 | eBPF监控、组件升级 |
| 阶段4 | 4.5 | 敏感信息泄露（运行时） | 日志脱敏、动态凭证、工作负载身份 |
| 阶段4 | 4.6 | 容器逃逸 | 权限裁剪、强制隔离、运行时监控 |
